# Генерация выборок зашумленных изображений

1. Переходим в директорию selection_generator: `cd selection_generator`
2. Собираем генератор: `make build`

# Зависимости
## Ubuntu 22.04
`apt install libboost-dev libboost-test-dev libpng-dev`